#pragma once

// POSIX mount point used by the Tab5 SD-card service. A future LVGL filesystem
// adapter may translate this path to a drive-letter path before image decode.
#define NABO_ASSET_ROOT "/sdcard/nabo"
